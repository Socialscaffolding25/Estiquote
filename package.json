{
  "name": "estiquote",
  "version": "1.0.0",
  "description": "UK home improvement cost intelligence platform",
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0",
    "stripe": "^14.0.0",
    "web-push": "^3.6.0",
    "resend": "^3.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  },
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  }
}